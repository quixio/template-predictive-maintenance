# custom.ini
[server]
http_port = 80

[paths]
provisioning = /app/state
data = /app/state

[users]
# Admin user with the password "adminpassword"
# Changed after startup. So dont bother trying it ;)
admin = admin